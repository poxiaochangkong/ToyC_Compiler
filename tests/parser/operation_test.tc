int main() {
  return 1 + 2 * 3 == 7 && 10 / 5 > 1 || !0;
  // 预期解析顺序:
  // 1. !0
  // 2. 2 * 3 -> 6
  // 3. 10 / 5 -> 2
  // 4. 1 + 6 -> 7
  // 5. 7 == 7 -> true
  // 6. 2 > 1 -> true
  // 7. true && true -> true
  // 8. true || true -> true
}